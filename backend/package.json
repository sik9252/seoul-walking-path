{
  "name": "seoul-walking-path-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "build:checkpoint-seed": "ts-node scripts/build-checkpoint-seed.ts",
    "build:checkpoint-upsert-sql": "ts-node scripts/build-checkpoint-upsert-sql.ts",
    "export:unresolved-checkpoints": "ts-node scripts/export-unresolved-checkpoints.ts",
    "suggest:checkpoint-overrides": "ts-node scripts/suggest-checkpoint-overrides.ts",
    "sync:tour-places": "ts-node scripts/sync-tour-places.ts",
    "sync:seoul-courses": "ts-node scripts/sync-seoul-courses.ts",
    "geocode:checkpoints:kakao": "ts-node scripts/geocode-checkpoints-kakao.ts",
    "sync:daily": "ts-node scripts/run-daily-sync.ts"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "@nestjs/swagger": "^11.2.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
